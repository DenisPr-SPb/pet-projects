services:
  oop-quotes:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - '5173:5173'
    environment:
      - CHOKIDAR_USEPOLLING=true
    working_dir: /app
    volumes:
      - .:/app